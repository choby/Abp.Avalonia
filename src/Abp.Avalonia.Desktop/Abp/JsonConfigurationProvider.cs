using Microsoft.Extensions.Configuration.Json;

namespace Abp.Avalonia.Desktop.Abp;

public class JsonConfigurationProvider1 : JsonConfigurationProvider
{
    public JsonConfigurationProvider1(JsonConfigurationSource source) : base(source)
    {
    }
}